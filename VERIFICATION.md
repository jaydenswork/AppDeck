# 验证记录 · 2026-09-08

## 2.4 原任务复用与标题缓存

设备仍为一加 PLK110 / Android 16。此处为真机任务信息、运行日志与实际投屏窗口的共同证据，下面 2.3 及更早记录为历史版本。

- 微信持续使用原 task 12487、原 LauncherUI ActivityRecord 255208800，发现页保持；书旗持续使用原 task 12484、原 ShuqiReaderActivity 21147642，阅读页与两层 Activity 栈保持。没有发送新的启动页 Intent，也没有杀死应用进程。
- 书旗：原阅读任务以竖屏接管后直接显示阅读页；关闭后，经 1920×1080 → 1080×1920 后台方向循环检查，Task ID 和 ActivityRecord 不变，每次归还成功。
- 阅读 io.legado.app.release：无已有任务时首次创建 task 12507，再以横屏接管仍为 task 12507，日志依次为 kind=new、kind=existing。
- 微信：正常入口接管、改名、关闭、重启主程序后再接管，缓存标题保持。修复归还顺序后，经 display 57 竖屏 → 58 横屏 → 59 集成竖屏连续检查；最终实际窗口仍在发现页，画面完整，没有此前的左侧黑区或裁切。
- 开发中发现 ColorOS 在横屏副屏上先恢复 fullscreen 会遗留 Activity Surface 横向偏移。最终改为先迁移到手机后台，再单独恢复原窗口配置。旧测试已污染的微信绘制偏移通过仅在临时副屏改变 letterbox 几何消除，未重建 Task 或 Activity；随后上述连续切换均正常。最终程序不包含临时诊断修复命令，不改主屏/全局兼容设置。
- 正常关闭后，微信、书旗、阅读各保留一个 Recent 任务卡片；手机主屏保持 home，旋转仍为 lock 0，已结束会话的虚拟屏和本地租借记录清理完成。
- 在投屏菜单保存“微信 · 标题缓存测试”，data/window-titles.xml 写入 com.tencent.mm 对应值；切换应用及完全退出后重启均读回。该测试缓存仅留在测试目录，不复制到用户项目的 data。
- x64 编译、既有自检和 Android 辅助程序编译通过；升级只复制改动的程序/源码/说明，不创建压缩包，不覆盖用户设置和收藏。

边界：突然拔线/系统强制回收时，系统可能自行把任务移回主屏前台；正常关闭路径先归还再销毁副屏。其他应用可能因 Android 配置变化自行重建页面。无运行任务但仅有系统持久化 Recent 的恢复分支、手机重启后的过期记录分支尚未单独实机覆盖；均没有强制新建重复任务的回退。未清理旧版本的其他历史任务，也未发送微信消息。

## 2.3 排版与退出修复

- 修正 2.2 的不完整验证：任务配置显示横屏不代表应用窗口完成横向布局；旧版在会话切换时仍可能出现小画面、裁切和黑边。
- 在同一台 PLK110／Android 16 上，新建集成会话 display 39（1080×1920）及 display 40（1920×1080），分别通过实际 Windows 投屏画面确认完整铺满；同时核对 WindowManager 的实际窗口 frame 为对应尺寸。
- 两次均由正常入口自动启动、配置和嵌入，不依赖额外手动修正。日志显示首次排版完成后再嵌入，约 2.5 秒。横屏、竖屏实际截图通过 Computer Use 直接检查，未留存聊天画面文件。
- 两次正常关闭后，虚拟显示器和微信活动均消失，手机主屏保留 home 任务，未弹回微信；主屏旋转仍为 lock 0。应用数据未清除。辅助程序拒绝主屏 ID 的调用。
- 去掉 --no-vd-destroy-content，使用 scrcpy 默认的销毁副屏内容行为，关闭时不再迁移任务或执行旧 ResetDisplay 清理。
- 自绘 SelectBox 改为同一 Form 内的连体面板，共用标题和列表外边框；设备、方向、尺寸和快捷设置复用。实测方向下拉展开、选择和收起正常。
- C# 构建和原有自检通过；Android helper 经 javac、D8 编译并用于上述实测。此版本直接更新项目 EXE、源码和辅助 jar，不生成压缩包。
- 未覆盖全部小说应用、每个应用的内部页面和跨 DPI 组合；同一应用在手机与副屏同时运行仍受其任务复用规则限制。以下为历史验证记录，以本节实际结果为准。
## 2.2 独立方向、托盘与快捷操作

- 微信在 PLK110／Android 16 副屏实际得到 land、1920×1080 配置，已查看真实横向排版；手机主屏仍为 lock 0、1272×2772。
- 仅固定副屏方向会产生竖屏黑边；副屏自由窗口模式配合任务区域后，微信横向排版生效。没有修改全局旋转或全局兼容开关。
- 集成版本建立副屏 24、25，生成 1920×1080 视频并成功嵌入。运行日志记录 Alt+W 隐藏／恢复，宿主 HWND 前后同为 6229778。
- ColorOS 退出后保留任务边界的问题已补上 WindowContainerTransaction 清理。辅助程序实际将本次微信测试任务的遗留 1920×1080 恢复为原生 1272×2772；正式入口拒绝主屏 ID，仅在关闭前清理自己的副屏。最终自动关闭链路未再次通过桌面操作复测。
- C# 编译和自检通过：应用筛选／参数校验、8 组尺寸映射、任务归属过滤；主屏、其他副屏、隐藏及尺寸正确的任务排除。
- Native 防抖测试通过：首次事件、490 个连续事件抑制、停顿重置、间隔边界与 INI 读取。
- 修改版 scrcpy 已用于实际微信会话。旧 MOD 快捷键和 F11 在输入层停用。总开关关闭时销毁 HotKey 接收器，没有全局键盘或鼠标 Hook。
- 14 张亮暗界面离线绘制通过，包含首页、投屏、方向尺寸、快捷键设置及标题栏检查。图片不是手机运行画面实测。
- 用户按 Esc 停止 Computer Use 后未继续桌面控制。未逐一实测所有小说软件的音量翻页、不同 Windows 设置下的任务栏／托盘表现，以及热键占用冲突场景。


## 2.1 窗口修复与横竖屏

- 设备选择行显式按容器高度布局，修复下边框被裁切。
- 原生窗口样式移除 caption / edge，接管非客户区重绘与激活绘制；取消 DWM 额外白色描边及 1px 玻璃扩展。保留自绘标题栏和细分割线。
- 侧栏图标缩小为 18 DIP，文字改为固定左对齐，缩短图文间距。深色面板改为接近用户提供 Codex 截图的中性深灰。
- 移除单独的微信小程序按钮。
- 新增显示方向，四档预设均可交换宽高；横屏使用对应宽窗口，选择保存在 settings.xml。改变已开窗口的尺寸或方向后再次打开，会正常结束原会话再重建。
- 关闭事件延迟到当前消息处理结束，避免无活动会话时重入 Close 导致窗口无法一次关闭。

本轮实际桌面检查使用程序的无手机预览窗口，通过 Computer Use 检查，不仅依赖控件位图：

| 检查 | 结果 |
| --- | --- |
| 首页亮 / 暗模式 | 只有一条自绘标题栏，暗色外围没有白圈 |
| 设备栏和侧栏 | 设备边框完整，图标和文字间距收紧，小程序按钮已移除 |
| 方向选择 | 在桌面实际点击横屏，标准尺寸即时从 1080×1920 更新到 1920×1080 |
| 横屏投屏窗口与菜单 | 宽窗口正常显示，单层标题栏、暗色边框和更多菜单正常 |
| 无会话关闭 | 修复后，点击横屏预览关闭按钮，进程退出，后续编译可直接写入 EXE |
| 参数和编译 | x64 编译及既有自检通过；新增四档 × 两方向的 8 个参数映射检查通过 |
| 控件离线渲染 | 12 个亮暗、正常 / 紧凑 / 横屏视图生成通过 |

本轮没有连接手机执行新的横屏副屏测试。手机应用是否支持横屏布局、跨 DPI 拖动和完整会话重建仍需用户复测。下列 2.0 记录为此前版本的历史证据。

## 2.0 界面更新

- 首页重建为分类侧栏、搜索栏、应用卡片、所选应用设置面板。
- 亮暗主题同步覆盖首页、投屏标题栏、菜单、无线连接和标题编辑，保存于便携设置。
- 投屏文字工具栏移除；返回、置顶、更多合并到自绘标题栏，其他操作进入菜单。
- 自绘下拉框、开关、图标按钮、滚动条；窄布局下设置面板可以滚动，打开按钮固定可见。
- 保留原有 ADB / scrcpy 会话流程及首帧后嵌入保护。全屏切换保持宿主 HWND 稳定，避免修改边框样式时销毁内嵌子窗口。

## 本次完成的验证

- Windows x64 编译通过，既有自检通过：应用 / 设备解析，系统应用在全部、搜索、收藏中的排除，阅读分类，参数转义，地址 / 包名校验，运行文件存在性。
- 程序控件离线渲染完成：亮 / 暗两套主题；首页默认 / 最小布局；投屏默认 / 窄窗布局；无线连接窗口，共 10 个视图。
- 校验自绘标题栏按钮在上述尺寸中不越界；人工检查输出图中的配色、文字、布局和图标。
- 预览使用明确标记的示例应用和原创示意文本，不是手机截图。未调用 ADB、不接管电脑输入。

用户暂时断开手机，要求修改后自行验证。因此 2.0 自绘标题栏下的实机投屏、跨显示器 DPI、拖动缩放、输入与重连由用户连接手机后验收；这里不把离线渲染或编译通过写成实机通过。

## 更新前已取得的实机证据

设备：OnePlus PLK110（一加 15），Android 16 / API 36，ColorOS V16.1.0，主屏 1272 × 2772 / 560 dpi。依赖为官方 scrcpy 4.1 Windows x64。

| 项目 | 已确认结果 |
| --- | --- |
| 应用枚举 | 172 个可启动应用，其中 151 个用户应用，排除 21 个系统应用 |
| 搜索与筛选 | 微信包名 1 条，系统设置包名 0 条，阅读分类 7 条；旧版首页实际搜索结果一致 |
| 主副屏任务 | 微信运行在虚拟显示器；主屏切换到桌面 / 设置时副屏继续保留微信 |
| 手机实体屏 | 基础验证中主屏 OFF、虚拟屏 ON；灭屏不等同于真实锁屏 |
| 成品窗口嵌入 | 修复首帧时序后，Texture 日志先出现，再完成 SDL 嵌入，微信位于 display 5 |
| 鼠标与滚轮 | 旧版成品内实际进入微信发现页并滚动到小程序入口；ADB input 记录对应虚拟屏点击 |
| 水铺子 | 在旧版成品窗口直接观察到“水铺子”首页；未下单、支付或发送消息 |
| 用户自测 | 用户反馈“功能基本没问题”，随后要求重做 UI |

## 已修复的底层问题

1. Windows 宿主同时提供 Path / PATH 时，.NET Framework 子进程环境枚举可能抛重复键异常。改为在 AppDeck 进程设置 ADB 路径，子进程直接继承环境；随后目录枚举成功。
2. 首帧前强行显示并调整 scrcpy 的隐藏 SDL 窗口，实测触发 scrcpy 除零崩溃（0xc0000094）。改为等待首帧 Texture 日志及 SDL 自身可见后再嵌入；修复后的会话成功输出微信画面。

无线连接、音频、其他输入法与小说应用的全部兼容组合没有逐项实机测试。
